<!DOCTYPE html>
<html>

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=0.8, maximum-scale=1">
  <title>Wedding Planning</title>
  <link rel="icon" href="/images/logo.png" type="image/png">
  <link rel="shortcut icon" type="image/png" href="/images/logo.png" />
  <link rel="stylesheet" href="/css/bootstrap.css">
  <link rel="stylesheet" href="/css/wedding_app.css">
</head>

<body class="p-3 m-0 border-0 bd-example">
  <nav class="navbar navbar-expand-lg bg-body-tertiary">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">Wedding Planning App</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup"
        aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
        <div class="navbar-nav mr-auto">
          <a class="nav-link active" aria-current="page" href="/">Home</a>
          <a class="nav-link" href="/venues">Venues</a>
          <a class="nav-link" href="/vendors">Vendors</a>
          <a class="nav-link" href="/budget">Budget Tracker</a>
          <a class="nav-link" href="/guestlist">Guestlist Manager</a>
          <a class="nav-link" href="/contact">Contact</a>
        </div>
        <div class="navbar-nav ms-auto">
          <% if (locals.loggedIn) { %>
            <div class="bubble logged-in">
              Logged in as <a href="<%=`/users/${locals.currentUser._id}`%>">
                <%= locals.currentUser.fullName %></a>
            </div>
            <div class="log-out">
              <a class="nav-link" href="/users/logout">Log out</a>
            </div>
          <% } else { %>
            <div class="bubble log-in">
              <a class="nav-link" href="/users/login">Log In</a>
            </div>
            <div class="bubble sign-up">
              <a class="nav-link" href="/users/new">Sign Up</a>
            </div>
          <% } %>
        </div>
      </div>
    </div>
  </nav>


  <div class="flashes">
    <% if (locals.flashMessages) { %>
      <% if (locals.flashMessages.success) { %>
        <% for(let message of locals.flashMessages.success) { %>
        <div class="flash success">
          <%= message %>
        </div>
        <% } %>
      <% } if (locals.flashMessages.error) { %>
        <% for(let message of locals.flashMessages.error) { %>
        <div class="flash error">
          <%= message %>
        </div>
        <% } %>
      <% } %>
    <% } %>
  </div>

  <%- body %>
  <script src="/js/venues.js"></script>
  <script src="/js/vendors.js"></script>
</body>

</html>